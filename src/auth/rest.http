@baseUrl = http://localhost:3000

###
# Auth local desativado. Use Supabase Auth para obter um JWT
# e envie nas requisições como Bearer token.
# Exemplo (cliente supabase-js no frontend):
# const { data: { session } } = await supabase.auth.signInWithPassword({ email, password })
# fetch('/user', { headers: { Authorization: `Bearer ${session.access_token}` } })
